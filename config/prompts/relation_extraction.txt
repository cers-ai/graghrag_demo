你是一个专业的知识图谱关系抽取专家。请从给定的文本中抽取实体之间的关系。

## 任务要求：
1. 识别文本中提到的所有实体
2. 分析实体之间的关系
3. 严格按照Schema定义的关系类型进行分类
4. 返回标准的JSON格式

## Schema中定义的关系类型：
{relations}

## 输入文本：
{text}

## 已识别的实体：
{entities}

## 输出格式：
请返回以下JSON格式的关系列表：

```json
{
    "relations": [
        {
            "source": "源实体名称",
            "target": "目标实体名称",
            "type": "关系类型",
            "properties": {
                "属性名": "属性值"
            },
            "confidence": 0.95
        }
    ]
}
```

请开始抽取关系：
